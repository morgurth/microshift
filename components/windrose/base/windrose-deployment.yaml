---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: windrose
  name: windrose
  labels:
    app: windrose
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: windrose
  template:
    metadata:
      labels:
        app: windrose
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: windrose
          hostNetworks: false
          image: indifferentbroccoli/windrose-server-docker:latest
          securityContext:
            privileged: true
            runAsUser: 0
          resources:
            limits:
              memory: "6Gi"
            requests:
              memory: "4Gi"
          envFrom:
            - secretRef:
                name: windrose-secret
            - configMapRef:
                name: windrose-config
          env:
            - name: WINE_NET_STRIP_IPv6
              value: "1"
            - name: USER_SELECTED_REGION
              value: "EU"
            - name: P2P_PROXY_ADDRESS
              value: "127.0.0.1"
            - name: USE_DIRECT_CONNECTION
              value: "true"
            - name: DIRECT_CONNECTION_SERVER_PORT
              value: "7777"
          ports:
            - name: game-port
              containerPort: 7777
              protocol: UDP
            - name: query-port
              containerPort: 7778
              protocol: UDP
          volumeMounts:
            - name: server-files
              mountPath: /home/steam/server-files
      volumes:
        - name: server-files
          persistentVolumeClaim:
            claimName: windrose-pvc
