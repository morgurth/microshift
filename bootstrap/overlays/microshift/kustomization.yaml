resources:
- ../../base

patches:
  - patch: |
      - op: replace
        path: /spec/source/path
        value: clusters/microshift/components/certificates
    target:
      kind: Application
      name: certificates
